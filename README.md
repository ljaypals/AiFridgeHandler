A fridge application that utilizes .Net 9 as the backend component and uses React and Kotlin for front end component. 

A user can manage a fridge by adding, removing and editing items in the app that represents item in a fridge. 
A user can also use such items and generate recipes that are suggested from the app.
A user can make an account and register into the app
